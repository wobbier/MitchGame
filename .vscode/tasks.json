{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-havana-editor-debug",
      "type": "shell",
      "command": "make",
      "args": [
        "-f",
        "MitchGame.make",
        "Havana",
        "config=editor_debug",
        "-j$(nproc)"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [
        "$gcc"
      ]
    }
  ]
}
